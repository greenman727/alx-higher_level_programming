#!/usr/bin/python3
def search_replace(my_list, search, replace):
    my_list = []
    for idx, search in my_list.index[search in enumerate(my_list):
            if mylist[idx] == search:
                my_list[idx] = replace
