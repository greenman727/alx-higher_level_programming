#!/usr/bin/python3
if __name__ == "__main__":
    def argument(*args):
        for i in args:
            if i == 1:
                print("{} argument".format(len(argv))
            else:
                print("{} arguments".format(len(argv))
