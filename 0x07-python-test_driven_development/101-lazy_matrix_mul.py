#!/usr/bin/python3
"""A module to multiply 2 matrices using the module NumPy"""
import numpy as np


def lazy_matrix_mul(m_a, m_b):
    """A function to return the multiplication of two matrices"""
    return (np.matmul(m_a, m-b))
